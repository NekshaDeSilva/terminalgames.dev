#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>
#include <vector>
#include <array>
#include <algorithm>

using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    tuple<int, string, string> mytuple make_tuple(1, "name", "apple");
    tuple<int, string, int> mytuple12 = make_tuple(42, "oouttuple", 42);
    tuple
}